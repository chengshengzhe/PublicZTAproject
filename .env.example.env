# 對外網域（敏感資料不建議放在repo。我們使用portainer與github連結來部署，所以敏感的變數仍放在github上，只是repo設定成私人）
ZTA_DOMAIN=your-domain.example

# Keycloak 管理員（帳號可不填，預設 admin）
KEYCLOAK_ADMIN_USERNAME=admin
KEYCLOAK_ADMIN_PASSWORD=change_me

# Keycloak DB 密碼（給 services: db & keycloak 共用）
KEYCLOAK_DB_PASSWORD=change_me

# Kong DB
KONG_PG_USER=kong
KONG_PG_DATABASE=kong
KONG_PG_PASSWORD=change_me

# API
JWT_SECRET=change_me
NAS_PATH=./NAS

# ELK（logstash_system / kibana_system）
ELK_PASSWORD=change_me

# Kong OIDC (Keycloak client)
KONG_OIDC_CLIENT_ID=kong
KONG_OIDC_CLIENT_SECRET=change_me
KONG_OIDC_SESSION_SECRET=change_me_at_least_32_chars

# Keycloak realm
KEYCLOAK_REALM=my-realm